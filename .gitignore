# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
env/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# mimule runtime state — these are generated by the fuzzer and shouldn't be
# checked in. Corpus, crashes, timeouts, coverage state, results.
corpus/
crashes/
timeouts/
divergences/
results/
logs/
coverage_state.pkl

# Target interpreter — cloned locally, not part of this repo
target/

# Tree-sitter compiled artifacts (if we end up using tree-sitter-monkey as fallback)
*.so
*.dylib
*.dll
